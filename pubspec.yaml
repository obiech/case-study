name: case_study
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  flutter_bloc: ^8.0.0
  dartz: ^0.10.1
  dio: ^4.0.0
  equatable: ^2.0.3
  freezed_annotation: ^1.0.0
  get_it: ^7.2.0
  injectable: ^1.5.0
  import_sorter: ^4.6.0
  # flutter_lints: ^1.0.0
  authentication_use_case:
    path: packages/auth_use_case
  authentication_infrastruture:
    path: packages/infrastruture
  authentication_domain:
    path: packages/domain
  utils:
    path: packages/utils


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.5
  freezed: ^1.0.2
  injectable_generator: ^1.5.2
  
flutter:
  uses-material-design: true